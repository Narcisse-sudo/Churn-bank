# Dépendances d'exécution (API + démo Streamlit) — utilisé par Streamlit Cloud & Docker.
# Versions de scikit-learn / xgboost alignées sur l'entraînement pour garantir le
# chargement du modèle sérialisé (artifacts/model.joblib).
numpy>=2.0,<3.0
pandas>=2.2,<3.0
scikit-learn>=1.7,<1.8
xgboost>=3.0,<4.0
joblib>=1.4
streamlit>=1.31
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pydantic>=2.6
